/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // If you want to use Unsplash images
  },
}

module.exports = nextConfig 